import React from "react";
import "./Order.scss";

const Order = () => {
  return (
    <div className={"order"}>
      <h4>Order</h4>
      <button className={"order__buy-btn"}>Buy</button>
    </div>
  );
};

export default Order;
